{
  "enabledPlugins": {
    "frontend-design@claude-plugins-official": true,
    "chrome-devtools-mcp@claude-plugins-official": true
  },
  "statusLine": {
    "type": "command",
    "command": "python -c \"import sys,json; d=json.load(sys.stdin); u=d.get('context_window',{}).get('used_percentage',0); u=int(u if u>=1 else u*100); m=d.get('model',{}).get('display_name',''); b=d.get('project',{}).get('git_branch',''); g=d.get('project',{}).get('git_status',{}); c=g.get('ahead',0)+g.get('behind',0)+len(g.get('modified',[]))+len(g.get('staged',[])); parts=[p for p in [m,f'Context: {u}%',f'{b} (+{c})' if c else b] if p]; print(' | '.join(parts))\""
  }
}
