{
  "enabledPlugins": {
    "frontend-design@claude-plugins-official": true,
    "chrome-devtools-mcp@claude-plugins-official": true
  },
  "statusLine": {
    "type": "command",
    "command": "python -c \"import sys,json; d=json.load(sys.stdin); E=chr(27); c=lambda co,t:f'{E}[{co}m{t}{E}[0m'; cw=d.get('context_window',{}); u=cw.get('used_percentage',0); u=int(u if u>=1 else u*100); m=d.get('model',{}).get('display_name',''); b=d.get('project',{}).get('git_branch',''); g=d.get('project',{}).get('git_status',{}); cnt=g.get('ahead',0)+g.get('behind',0)+len(g.get('modified',[]))+len(g.get('staged',[])); fmt=lambda n: f'{int(n)/1e6:.2f}M' if n and n>=1e6 else f'{int(n)/1e3:.1f}k' if n and n>=1e3 else str(int(n)) if n is not None else None; cu=cw.get('current_usage') or {}; tot=fmt(cw.get('total_output_tokens')); cur=fmt(cu.get('output_tokens')); ctxco='91;1' if u>=85 else '93;1' if u>=70 else '93' if u>=50 else '92'; bp=f'{b} (+{cnt})' if cnt else b; tk=[c('90',k)+' '+v for k,v in [('out',tot),('gen',cur)] if v]; toks=' '.join(tk); parts=[p for p in [c('96;1',m) if m else '',c(ctxco,f'Context: {u}%'),c('95',bp) if bp else '',toks] if p]; print(' | '.join(parts))\""
  }
}
